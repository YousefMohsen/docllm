DATABASE_URL=postgresql://postgres:password@127.0.0.1:5433/document_metadata
DOCUMENTS_PATH=./documents

# MCP server (Phase 3)
MCP_SERVER_NAME=document-search

# OpenAI (Phase 2: entity extraction)
OPENAI_API_KEY=your_openai_api_key_here
EXTRACTION_DELAY_MS=200
EXTRACTION_MODEL=gpt-4o-mini
EXTRACTION_BATCH_SIZE=10
EXTRACTION_MAX_CHARS=100000

# OpenAI (Phase 5: embeddings + semantic search)
EMBEDDING_MODEL=text-embedding-3-small
CHUNK_SIZE_TOKENS=700
CHUNK_OVERLAP_TOKENS=100
EMBEDDING_BATCH_SIZE=50
EMBEDDING_DELAY_MS=100
IVFFLAT_PROBES=50
